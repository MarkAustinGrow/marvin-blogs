<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    header {
      margin-bottom: 30px;
      border-bottom: 1px solid #eee;
      padding-bottom: 20px;
    }
    h1 {
      color: #2c3e50;
    }
    .back-link {
      display: inline-block;
      margin-bottom: 20px;
      color: #3498db;
      text-decoration: none;
    }
    .back-link:hover {
      text-decoration: underline;
    }
    .create-form {
      background-color: #f9f9f9;
      padding: 30px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .form-description {
      margin-bottom: 20px;
    }
    .submit-btn {
      background-color: #3498db;
      color: white;
      padding: 10px 15px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
    }
    .submit-btn:hover {
      background-color: #2980b9;
    }
    .error {
      color: #e74c3c;
      padding: 10px;
      background-color: #fadbd8;
      border-radius: 4px;
      margin-bottom: 20px;
    }
    .loading {
      display: none;
      margin-top: 20px;
      text-align: center;
    }
    .loading p {
      margin-bottom: 10px;
    }
    .spinner {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #3498db;
      border-radius: 50%;
      width: 30px;
      height: 30px;
      animation: spin 2s linear infinite;
      margin: 0 auto;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <header>
    <h1><%= title %></h1>
  </header>
  
  <main>
    <a href="/" class="back-link">← Back to Blog Posts</a>
    
    <% if (typeof error !== 'undefined') { %>
      <div class="error">
        <%= error %>
      </div>
    <% } %>
    
    <div class="create-form">
      <div class="form-description">
        <p>Clicking the button below will trigger the Marvin Blogger Agent to generate a new blog post based on tweets, art, and memory insights.</p>
        <p><strong>Note:</strong> This process may take a few minutes to complete.</p>
      </div>
      
      <form id="createForm" action="/create" method="POST">
        <button type="submit" class="submit-btn" id="submitBtn">Generate New Blog Post</button>
      </form>
      
      <div class="loading" id="loadingIndicator">
        <p>Generating blog post... This may take a few minutes.</p>
        <div class="spinner"></div>
      </div>
    </div>
  </main>
  
  <script>
    document.getElementById('createForm').addEventListener('submit', function() {
      document.getElementById('submitBtn').style.display = 'none';
      document.getElementById('loadingIndicator').style.display = 'block';
    });
  </script>
</body>
</html>
